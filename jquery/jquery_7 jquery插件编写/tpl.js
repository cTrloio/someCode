/**
 * Created by Administrator on 2016/6/15.
 */
//官方模板
;(function ($) {
    $.fn.plugin = function (options) {
        var defaults = {
            //各种参数和属性
        }
        var options = $.extend(defaults,options);

        this.each(function () {
            //实现功能代码
        });
    }
})(jQuery);